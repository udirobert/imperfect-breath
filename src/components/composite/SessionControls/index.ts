/**
 * SessionControls Component Export
 * 
 * Unified export for the consolidated SessionControls component
 */

export { SessionControls as default } from "./SessionControls";
export { SessionControls } from "./SessionControls";
export type { 
  SessionControlsProps, 
  SessionMetrics,
  SessionPattern,
  SessionControlVariant,
  SessionState,
  SessionMode
} from "./types";
export { 
  SESSION_CONTROL_CONFIGS, 
  VARIANT_STYLES, 
  FLOATING_POSITIONS,
  STATE_COLORS,
  PHASE_COLORS 
} from "./variants";